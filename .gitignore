# dependencies
node_modules/
pipeline/.venv/
pipeline/__pycache__/
**/__pycache__/

# next.js
.next/
out/
next-env.d.ts

# env
.env.local
.env*.local

# data / cache
data/pdfs/
data/cache/*
# The OCR cache IS committed: Surya OCR is deterministic and CPU-expensive, so
# committing the text output lets CI (and fresh clones) skip re-OCR entirely.
!data/cache/ocr/
*.log

# misc
.DS_Store
.vercel
coverage/
*.tsbuildinfo

# local artifacts
.playwright-mcp/
*.jpeg
