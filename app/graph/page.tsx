import { Placeholder } from "@/components/Placeholder";

export const metadata = { title: "Graph" };

export default function GraphPage() {
  return (
    <Placeholder title="Knowledge graph" phase="Phase 3">
      A force-directed knowledge graph linking the President, companies, sectors, trades,
      statements, and official actions — explorable by expanding any node.
    </Placeholder>
  );
}
