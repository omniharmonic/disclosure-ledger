import { Placeholder } from "@/components/Placeholder";

export const metadata = { title: "Timeline" };

export default function TimelinePage() {
  return (
    <Placeholder title="Timeline" phase="Phase 3">
      A multi-lane interactive timeline plotting trades, statements, and official actions on
      parallel tracks, so the sequence around any trade is legible at a glance.
    </Placeholder>
  );
}
